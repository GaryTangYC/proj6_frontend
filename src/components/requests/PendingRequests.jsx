/* mui imports */
import { Typography } from "@mui/material";

export default function PendingRequests() {
  return (
    <div>
    <Typography variant="body1">This is pending requests page</Typography>
    </div>
  );
}
